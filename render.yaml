services:
  - type: web
    name: zgrzyt-api
    runtime: docker

  - type: web
    name: zgrzyt-websockets
    runtime: docker
    dockerCommand: php artisan reverb:start --host=0.0.0.0 --port=$PORT
